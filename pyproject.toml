[project]
name = "enpm661-project-02"
version = "0.1.0"
description = "Backward BFS and Backward Dijkstra path planning for ENPM661 Project 02."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
